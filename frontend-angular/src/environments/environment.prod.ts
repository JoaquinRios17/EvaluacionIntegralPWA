// se usa en el build de produccion (npm run build) que se sube a Vercel
// IMPORTANTE: reemplazar por la URL real del backend en Render antes de desplegar
export const environment = {
  production: true,
  apiUrl: 'https://gestion-cursos-api.onrender.com/api',
};
